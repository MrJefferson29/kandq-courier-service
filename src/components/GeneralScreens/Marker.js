import React from 'react';

const CustomMarker = ({ position, title }) => {
  return (
    <div className="custom-marker">
      <div className="marker-title">This is Your Package</div>
    </div>
  );
};

export default CustomMarker;